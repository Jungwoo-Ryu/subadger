# Monorepo root: Vercel entry when Root Directory is the repository root (not `backend`).
[project]
name = "subadger"
version = "0.1.0"
requires-python = ">=3.12"

[project.scripts]
app = "index:app"
